# Портфолио Платона Акулова - Готово к развёртке! 🚀

**Статус:** ✅ Проект готов к загрузке на Reg.ru shared-хостинг  
**Домен:** platon-akulov.ru  
**СУБД:** SQLite3  
**DEBUG:** True (для отладки на сервере)  

---

## 📋 Быстрый старт к развёртке

### 1. Финальная подготовка локально (Windows):
```bash
prepare_deployment.bat
```

Или вручную:
```bash
python manage.py collectstatic --noinput
python check_deployment.py
```

### 2. Что было обновлено в конфигурации:
- ✅ **Домен:** `platonakulov.ru` → `platon-akulov.ru`
- ✅ **ALLOWED_HOSTS:** обновлены на новый домен
- ✅ **SECRET_KEY:** изменён на уникальный
- ✅ **passenger_wsgi.py:** подготовлен для Reg.ru (пути нужно обновить вручную)
- ✅ **DEBUG:** оставлен True для отладки на сервере

### 3. Перед загрузкой на хостинг:

**Важно!** Отредактируйте файл `passenger_wsgi.py`:
```python
# Замените на ваше имя пользователя из Reg.ru
SITE_ROOT = '/home/YOUR_USERNAME/public_html'
VENV_PATH = '/home/YOUR_USERNAME/public_html/venv/lib/python3.10/site-packages'
```

### 4. Загрузка на хостинг Reg.ru:
1. Используйте FTP (FileZilla, WinSCP, Putty и т.д.)
2. Загрузите содержимое проекта в `/public_html/` вашего домена
3. **НЕ загружайте:** `venv/`, `.git/`, `__pycache__/`, `*.pyc`

### 5. На сервере (через SSH/консоль):
```bash
cd ~/public_html
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate (если нужно)
touch tmp/restart.txt
```

---

## 📁 Созданные файлы для развёртки:

| Файл | Описание |
|------|---------|
| `DEPLOYMENT_REGRU.md` | 📖 Полная пошаговая инструкция развёртки |
| `check_deployment.py` | ✅ Скрипт проверки конфигурации |
| `prepare_deployment.bat` | ⚡ Скрипт подготовки (Windows) |
| `prepare_deployment.sh` | ⚡ Скрипт подготовки (Linux/Mac) |
| `.env.example` | 🔐 Пример файла окружения |
| `passenger_wsgi.py` | 🔧 Обновлённый файл для Passenger |
| `portfolio/settings.py` | ⚙️ Обновлённые настройки Django |

---

## ⚙️ Ключевые параметры:

### Домен
- Primary: `platon-akulov.ru`
- Redirect: `www.platon-akulov.ru`

### Директории
- Корень: `/home/USERNAME/public_html/`
- Статические файлы: `/collected_static/`
- Загрузки: `/media/`
- БД: `db.sqlite3` (в корне)

### Хостинг Reg.ru
- Тип: Shared hosting (без root)
- Приложение: Passenger (WSGI)
- Python: 3.8+ (уточните в поддержке)
- SSH: Доступен для управления

---

## 🔧 Что делать после развёртки:

### 1. Проверить работу:
```bash
# На хостере
curl https://platon-akulov.ru
# Должна загрузиться главная страница
```

### 2. Проверить админку:
```
https://platon-akulov.ru/admin/
# Если миграции успешны
```

### 3. Мониторить логи:
```bash
# На сервере
tail -f passenger_wsgi.log
tail -f django.log
```

### 4. Если проблемы:
- 500 ошибка? → Проверьте `passenger_wsgi.log`
- Статика не грузится? → Запустите `collectstatic` заново
- БД не найдена? → Проверьте права доступа: `chmod 666 db.sqlite3`

---

## 📞 Полезные команды на сервере:

```bash
# Активировать venv
source ~/public_html/venv/bin/activate

# Запустить консоль Django
python manage.py shell

# Перезагрузить приложение
touch ~/public_html/tmp/restart.txt

# Проверить логи
tail -100 ~/public_html/passenger_wsgi.log

# Очистить кэш Python
find ~/public_html -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null

# Собрать статику
python manage.py collectstatic --noinput
```

---

## 🆘 Частые проблемы и решения:

### ❌ "ModuleNotFoundError: No module named 'django'"
```bash
# Убедитесь, что venv активирован и зависимости установлены
source venv/bin/activate
pip install -r requirements.txt
```

### ❌ "PermissionError: [Errno 13] Permission denied: 'db.sqlite3'"
```bash
# Выдайте права на запись
chmod 666 db.sqlite3
chmod 755 .
```

### ❌ "ImproperlyConfigured: You haven't set the SECRET_KEY setting yet"
```python
# В settings.py убедитесь, что SECRET_KEY не пустой
SECRET_KEY = 'your-secret-key-here'
```

### ❌ Статические файлы возвращают 404
```bash
# Пересобрать статику
python manage.py collectstatic --noinput --clear
```

---

## 📚 Документация:

- **Подробная инструкция:** `DEPLOYMENT_REGRU.md`
- **Проверка конфигурации:** `python check_deployment.py`
- **Документация Reg.ru:** https://help.reg.ru/
- **Django документация:** https://docs.djangoproject.com/

---

## ✨ Следующие шаги:

1. ✅ Читаем `DEPLOYMENT_REGRU.md` полностью
2. ✅ Обновляем пути в `passenger_wsgi.py`
3. ✅ Загружаем проект на хостинг через FTP
4. ✅ Настраиваем через SSH
5. ✅ Проверяем, что работает
6. ✅ Мониторим логи
7. ✅ Радуемся! 🎉

---

**Создано:** November 18, 2025  
**Версия Django:** 5.1.7  
**Python:** 3.10+  

**Проект готов к запуску! Удачи! 🚀**
