# Инструкция развёртки на Reg.ru (Shared Hosting)

## Информация о проекте
- **Домен:** platon-akulov.ru
- **Хостинг:** Reg.ru Shared (без root доступа)
- **СУБД:** SQLite3
- **DEBUG:** True (для отладки на сервере)
- **Python:** 3.10+

---

## Шаг 1: Подготовка к развёртке

### 1.1 Получите от Reg.ru:
- Имя пользователя (username)
- FTP/SFTP доступ
- SSH доступ (если доступен)
- Путь к public_html (обычно `/home/USERNAME/public_html`)
- Установленную версию Python (уточните)

### 1.2 Уточните структуру хостинга в поддержке:
```
/home/USERNAME/
├── public_html/          # Корневая папка сайта (DOCUMENT_ROOT)
├── private_html/         # Приватная папка (если нужна)
└── tmp/                  # Временные файлы
```

---

## Шаг 2: Загрузка файлов на хостинг

### 2.1 Через FTP (FileZilla, WinSCP и т.д.):
1. Подключитесь к FTP: `ftp.platon-akulov.ru` (или IP сервера)
2. Загрузите все файлы проекта в `/home/USERNAME/public_html/`
   - ✅ Загрузите: `manage.py`, `portfolio/`, `main/`, `db.sqlite3`, `requirements.txt` и т.д.
   - ❌ НЕ загружайте: `venv/`, `.git/`, `__pycache__/`, `*.pyc`

### 2.2 Структура на сервере:
```
/home/USERNAME/public_html/
├── manage.py
├── passenger_wsgi.py       ← Главный WSGI файл для Passenger
├── requirements.txt
├── db.sqlite3              ← База данных SQLite
├── portfolio/              ← Папка конфига Django
│   ├── __init__.py
│   ├── settings.py         ← ✅ Уже обновлена для нового домена
│   ├── urls.py
│   ├── wsgi.py
│   └── asgi.py
├── main/                   ← Приложение Django
├── static/                 ← Статические файлы
├── collected_static/       ← Собранные статические файлы
├── media/                  ← Загружаемые файлы
└── venv/                   ← Виртуальное окружение (создастся на сервере)
```

---

## Шаг 3: Конфигурация на сервере (через SSH/консоль)

### 3.1 Подключитесь по SSH:
```bash
ssh username@platon-akulov.ru
# или
ssh username@IP_ADDRESS
```

### 3.2 Перейдите в папку проекта:
```bash
cd ~/public_html
```

### 3.3 Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # На Linux/Mac
# Или на Windows (если есть):
venv\Scripts\activate
```

### 3.4 Установите зависимости:
```bash
pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
```

### 3.5 Обновите пути в `passenger_wsgi.py`:

**Важно!** Отредактируйте файл `passenger_wsgi.py` и замените:
```python
SITE_ROOT = '/home/USER/public_html'  # ← Вместо USER поставьте ваше имя пользователя
VENV_PATH = '/home/USER/public_html/venv/lib/python3.10/site-packages'
```

Узнайте версию Python на сервере:
```bash
python3 --version
```

---

## Шаг 4: Собрать статические файлы

### 4.1 Запустите команду сбора статики:
```bash
python manage.py collectstatic --noinput
```

Это поместит все CSS, JS, изображения в папку `collected_static/`

---

## Шаг 5: Создайте/обновите `.htaccess`

Создайте файл `.htaccess` в корне (`/home/USERNAME/public_html/.htaccess`):

```apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # Перенаправляем HTTP на HTTPS (если нужно)
    # RewriteCond %{HTTPS} off
    # RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
    
    # Направляем все запросы через passenger_wsgi.py
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
    PassengerEnabled On
    PassengerAppType wsgi
    PassengerStartupFile passenger_wsgi.py
    PassengerPython /home/USERNAME/public_html/venv/bin/python
</IfModule>

# Кэширование статических файлов
<FilesMatch "\.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$">
    Header set Cache-Control "max-age=31536000, public"
</FilesMatch>

# Отключаем доступ к приватным файлам
<FilesMatch "(manage\.py|settings\.py|requirements\.txt|\.env|\.env\.example|passenger_wsgi\.log|django\.log)$">
    Order allow,deny
    Deny from all
</FilesMatch>
```

**Важно:** Замените `USERNAME` на ваше имя пользователя на хостере!

---

## Шаг 6: Инициализация БД (если нужно)

Если вы первый раз развёртываете:
```bash
python manage.py migrate
python manage.py createsuperuser  # Создаёте админа для /admin
```

---

## Шаг 7: Перезагрузка Passenger

После загрузки файлов, обновления `.htaccess` или изменения кода:

### Способ 1 - Через консоль:
```bash
touch ~/public_html/tmp/restart.txt
```

### Способ 2 - Через панель управления Reg.ru:
1. Перейдите в панель управления
2. Найдите раздел "Python" или "Приложения"
3. Перезагрузите приложение

### Способ 3 - Загрузите пустой файл `restart.txt`:
```
Через FTP загрузите пустой файл в: /home/USERNAME/tmp/restart.txt
```

---

## Шаг 8: Проверка работы

1. Откройте в браузере: `https://platon-akulov.ru`
2. Проверьте логи ошибок:
   - На сервере: `passenger_wsgi.log` в корне проекта
   - Django логи: `django.log` в корне проекта
   - Логи Reg.ru: в панели управления (Error logs)

3. Проверьте панель администратора:
   - `https://platon-akulov.ru/admin` (если проверили migrate)

---

## Решение проблем

### ❌ Ошибка 500 Internal Server Error
1. Проверьте логи: `cat passenger_wsgi.log`
2. Убедитесь, что `settings.py` имеет правильный домен: `platon-akulov.ru`
3. Проверьте пути в `passenger_wsgi.py`
4. Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`

### ❌ Статические файлы не загружаются (404)
1. Запустите: `python manage.py collectstatic --noinput`
2. Проверьте права доступа на папку `collected_static/`:
   ```bash
   chmod 755 collected_static/
   ```
3. Убедитесь, что в `.htaccess` правильные пути

### ❌ БД не найдена
1. Проверьте, что `db.sqlite3` загружена на сервер
2. Проверьте права доступа:
   ```bash
   chmod 666 db.sqlite3
   ```

### ❌ Python не найден
1. Узнайте правильный путь:
   ```bash
   which python3
   which python
   ```
2. Обновите пути в `passenger_wsgi.py`

### ❌ Модули Python не найдены
1. Убедитесь, что venv активирован при установке пакетов
2. Переустановите зависимости:
   ```bash
   source venv/bin/activate
   pip install -r requirements.txt --force-reinstall
   ```

---

## Важные замечания

### 🔐 Безопасность
- ✅ `DEBUG = True` оставлено для отладки - ОЧЕНЬ рекомендуется поменять на `False` в production
- ✅ `SECRET_KEY` — измените на надёжный ключ в `settings.py`
- ✅ Отключите доступ к управлению приватным файлам через `.htaccess`

### 📊 Мониторинг
- Следите за логами: `tail -f passenger_wsgi.log`
- Проверяйте размер БД: `ls -lah db.sqlite3`
- Для production рекомендуется PostgreSQL вместо SQLite

### 🔄 Обновление кода
1. Загрузите новые файлы через FTP
2. Запустите: `python manage.py migrate` (если были миграции)
3. Запустите: `python manage.py collectstatic --noinput`
4. Перезагрузите Passenger: `touch tmp/restart.txt`

---

## Полезные команды для отладки

```bash
# Проверить версию Python
python3 --version

# Активировать виртуальное окружение
source venv/bin/activate

# Запустить Django оболочку (на сервере)
python manage.py shell

# Запустить тесты
python manage.py test

# Очистить кэш Django
python manage.py clear_cache

# Мониторить логи в реальном времени
tail -f passenger_wsgi.log
tail -f django.log

# Проверить размер БД
du -sh db.sqlite3

# Установить права на папки
chmod 755 . static/ collected_static/ media/
chmod 644 db.sqlite3
```

---

## Контакты поддержки
- **Reg.ru:** https://help.reg.ru/ (документация и чат)
- **Панель:** https://www.reg.ru/ (личный кабинет)

---

**Проект готов к развёртке! Удачи! 🚀**
